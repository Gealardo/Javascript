"use strict"; // code will be treated by new js rules-> even its default as well

// alert(3+1); we are using node js not browser (in brower output will be 4)


//priority -> code redability
// console.log("ayun") console.log("ayush") "ERROR" 
  console.log("ayun"); console.log("ayush")// "NO ERROR" but not a good practice (write in next line)


let name="ayun";
let age=21;  
let postGraguated= false;
let children=null; // initialized with empty .(object)
let college; // not even initialized after declaration.(datatype->undefined itself)

//symbol
//number
//bigint
//object



console.log(typeof(x));
console.table([name,age,postGraguated,children,college]);
console.table([typeof(name),typeof[age],typeof(postGraguated),typeof[children],typeof(college)]);






